插件disk_fs:

功能介绍：
  本插件用于检查磁盘各分区的文件系统状态和读写测试

使用介绍：
  此插件共有一个参数：
  filesystem_fstype     只检查指定文件系统类型的磁盘分区
可以通过命令行工具eminfo config 手工配置以上1个参数

配置实例：
  # eminfo u disk_fs filesystem_fstype "ext2 ext3 ext4"
  # eminfo u disk_fs filesystem_fstype "ext2 ext3 ext4 nfs"

测试运行：
  # eminfo run disk_fs
