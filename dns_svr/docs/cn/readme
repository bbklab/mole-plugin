插件dns_svr:
 
功能介绍：
  本插件用于检查DNS解析服务，通过模拟DNS解析来测试DNS解析结果和解析速度
 
使用介绍：
  此插件共有两个参数：
  dns_check_lst		要测试的DNS解析对象和类型.    格式:  对象1:类型1,类型2,类型3   对象2:类型1,类型2
  max_elapsed_time	解析每组DNS对象的最大超时时间(单位秒)
可以通过命令行工具eminfo config 手工配置以上2个参数
 
配置实例：
  # eminfo u dns_svr dns_check_lst "eyou.net:mx,ns,soa  mail.eyou.net:a  8.8.8.8:ptr"
  # eminfo u dns_svr max_elapsed_time 10
  
测试运行： 
  # eminfo run dns_svr 
