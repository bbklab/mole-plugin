插件notify_syslogin:
 
功能介绍：
  本插件用于发现和提醒系统用户登录事件
 
使用介绍：
  此插件共有一个参数：
  authfile		从哪个文件中抓取系统用户登录事件
可以通过命令行工具eminfo config 手工配置以上1个参数
 
配置实例：
  # eminfo u notify_syslogin authfile "/var/log/secure"
  
测试运行： 
  # eminfo run notify_syslogin 
